[build]
  command = "npm run build"
  publish = "dist"
  functions = "server/functions"
  node_version = "22"

[dev]
    command = "npm run serve"
    port = 8887
    functions = "server/functions"
    framework = "#auto"
    
[functions]
  [functions.max_duration]
    duration = 60
